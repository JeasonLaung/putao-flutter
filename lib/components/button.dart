
import 'package:flutter/material.dart';

class SuiButton extends StatelessWidget {

  final double width;
  final String height;
  SuiButton({this.width, this.height});
  @override
  Widget build(BuildContext context) {
    return GestureDetector(
    );
  }
}